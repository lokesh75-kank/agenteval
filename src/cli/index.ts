#!/usr/bin/env node
// AgentEval CLI entry point. Implemented in Phase 2 (run | baseline | check | init).
// Placeholder so the build graph resolves during scaffolding.

export {};

if (process.argv[1]) {
  // eslint-disable-next-line no-console
  console.log('agenteval CLI - not yet implemented');
}
