// AgentEval MCP server. Implemented in Phase 2.
// Exposes evaluate_agent | check_grounding | get_report so coding agents
// (Claude, Codex) can call AgentEval directly as a tool.
// Placeholder so the build graph resolves during scaffolding.

export {};
